# Local development uses the Vite API proxy.
VITE_API_URL=/api
# For separate production hosting, set VITE_API_URL=https://api.ravimenu.com/api
# and VITE_BACKEND_URL=https://api.ravimenu.com before building.
